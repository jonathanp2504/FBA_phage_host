[1mdiff --git a/dFBA_function.jl b/dFBA_function.jl[m
[1mindex cb24baa..19bc4a5 100644[m
[1m--- a/dFBA_function.jl[m
[1m+++ b/dFBA_function.jl[m
[36m@@ -1,30 +1,100 @@[m
 function dFBA_phage_system(du, u, h, p::Parameters, t)[m
  [m
[31m-    # --- EXTRACTIE ---[m
[32m+[m[32m    # --- 1. EXTRACTIE ---[m
     S_subs = u[p.ind_subs][m
     e_enz  = u[p.ind_e][m
[31m-    S_cell, I_cell, L_cell, P_phage = u[p.ind_S], u[p.ind_I], u[p.ind_L], u[p.ind_P][m
[31m-    C_benz = u[p.ind_Benz] # De 13e variabele in u is de benzonase concentratie[m
[31m-    # --- 1. CYBERNETICA ---[m
[32m+[m[32m    I_cell, L_cell, P_phage = u[p.ind_I], u[p.ind_L], u[p.ind_P][m
[32m+[m[32m    C_benz = u[p.ind_Benz][m
[32m+[m[32m    e_mal  = u[p.ind_e[2]] # LamB proxy (Maltose enzym)[m
[32m+[m
[32m+[m[32m    # --- 2. CYBERNETICA & FBA ---[m
     f = getMonod(S_subs, p)[m
     R = getRate(f, p)[m
     u_cyt = getU_cyt(R, p)[m
[31m-    v_cyt = getV_cyt(R) # <--- De rode lijn zou nu moeten verdwijnen[m
[32m+[m[32m    v_cyt = getV_cyt(R)[m
 [m
[31m-   [m
[31m-    phi = getPhi(u, p)[m
[31m-    prob_lys = getProbLys(phi)[m
[32m+[m[32m    fbaUpdate!(u, p) # Berekent p.mu en p.q_benz[m
[32m+[m[32m    X_tot = getTotalBiomass(u, p)[m
[32m+[m
[32m+[m[32m    # --- 3. ADSORPTIE LADDER LOGICA (Sequential Model) ---[m
[32m+[m[32m    # Effectieve capaciteit o.b.v. LamB (e_mal)[m
[32m+[m[32m    eff_cap = p.max_phages * (e_mal / (p.K_mal + e_mal + 1e-15))[m
     [m
[31m-    X_tot                 = getTotalBiomass(u, p)[m
[31m-    totaal_adsorptie_flux = getTotalAdsorptionFlux(u, p)[m
[31m-    nieuwe_infectie_flux  = getNewInfectionFlux(u, p)[m
[31m-    u_p = h(p, t - p.tau)[m
[31m-    lysis_term = t > p.tau ? getNewInfectionFlux(u_p, p) : 0.0[m
[31m-    mu_safe = max(0.0, p.mu) # Zorg dat groeisnelheid niet negatief wordt, met toevoeging van mu_safe crasht simulatie[m
[31m-    # --- 2. TOXICITEIT BEREKENEN ---[m
[31m-    # We berekenen de sterfte door Benzonase[m
[32m+[m[32m    total_new_infections = 0.0[m
[32m+[m[32m    total_ads_loss = 0.0 # We houden bij hoeveel fagen er totaal adsorberen (verlies voor P, want niet meer vrij in medium)[m[41m [m
[32m+[m
[32m+[m[32m    # We loopen door de ladder van S0 t/m S_max[m
[32m+[m[32m    for i in 0:p.max_phages[m
[32m+[m[32m        idx = p.ind_S_ladder[i+1][m[41m [m
[32m+[m[32m        S_i = u[idx][m
[32m+[m
[32m+[m[32m        # A. TOXICITEIT BEREKENEN VOOR DEZE TREDE[m
[32m+[m[32m        # S-cellen zijn 10x minder gevoelig dan L-cellen volgens jouw code[m
[32m+[m[32m        toxic_death_S_i = (p.k_tox * 0.1) * C_benz * S_i[m
[32m+[m
[32m+[m[32m        # B. BINDING (S_i -> S_i+1)[m
[32m+[m[32m        v_bind = 0.0 # snelheid waarmee cellen extra faag binden en naar de volgende trede gaan[m
[32m+[m[32m        if i < p.max_phages[m
[32m+[m[32m            binding_multiplier = max(0.0, (eff_cap - i) / p.max_phages)[m
[32m+[m[32m            v_bind = p.k_on * P_phage * S_i * binding_multiplier[m
[32m+[m[41m            [m
[32m+[m[32m            du[idx] -= v_bind[m
[32m+[m[32m            du[idx+1] += v_bind[m[41m [m
[32m+[m[32m            total_ads_loss += v_bind[m
[32m+[m[32m        end[m
[32m+[m
[32m+[m[32m        # C. LOSKOPPELING (S_i -> S_i-1)[m
[32m+[m[32m        v_off = 0.0 # snelheid waarmee fagen loskomen van cellen en terug naar de vorige trede gaan (want nog niet geinfecteerd dus reversible)[m
[32m+[m[32m        if i > 0[m
[32m+[m[32m            v_off = p.k_off * i * S_i[m
[32m+[m[32m            du[idx] -= v_off[m
[32m+[m[32m            du[idx-1] += v_off[m[41m [m
[32m+[m[32m            total_ads_loss -= v_off[m
[32m+[m[32m        end[m
[32m+[m
[32m+[m[32m        # D. INJECTIE (S_i -> I)[m
[32m+[m[32m        v_ins = 0.0[m
[32m+[m[32m        if i > 0[m
[32m+[m[32m            v_ins = p.k_ins * i * S_i[m
[32m+[m[32m            du[idx] -= v_ins[m
[32m+[m[32m            total_new_infections += v_ins[m
[32m+[m[32m        end[m
[32m+[m
[32m+[m[32m        # E. BALANS VOOR S_i (Groei - Dood door Benz)[m
[32m+[m[32m        # We voegen hier de mu * S_i en de toxic_death_S_i samen[m
[32m+[m[32m        du[idx] += p.mu * S_i - toxic_death_S_i[m
[32m+[m[32m    end[m
[32m+[m
[32m+[m[32m    # --- 4. PHAGE-HOST INTERACTIE ---[m
[32m+[m[32m    # Vertraagde Lysis (gebaseerd op succesvolle injecties uit het verleden)[m
[32m+[m[32m    #u_p = h(p, t - p.tau)[m
[32m+[m[32m    # Sommatie van k_ins * i * S_i op tijdstip t-tau[m
[32m+[m[32m    #total_ins_past = sum([p.k_ins * i * u_p[p.ind_S_ladder[i+1]] for i in 1:p.max_phages])[m
[32m+[m[32m    #lysis_term = t > p.tau ? total_ins_past : 0.0[m
[32m+[m[32m    # Efficiëntere berekening van de delay term[m
[32m+[m[32m    # 1. Definieer de lysis_term met het juiste type (Float64 of Dual)[m
[32m+[m[32m    lysis_term = 0.0[m
[32m+[m[32m    if t > p.tau[m
[32m+[m[32m        u_p = h(p, t - p.tau)[m
[32m+[m[32m    # Loop handmatig door de ladder voor snelheid en stabiliteit[m
[32m+[m[32m        for i in 1:p.max_phages[m
[32m+[m[32m        # Veiligheidscheck: zorg dat we niet buiten de u_p vector indexeren[m
[32m+[m[32m        val_past = u_p[p.ind_S_ladder[i+1]][m
[32m+[m[32m        lysis_term += p.k_ins * i * max(0.0, val_past)[m
[32m+[m[32m        end[m
[32m+[m[32m    end[m
[32m+[m
[32m+[m[32m    # Lysogenie beslissing (gebaseerd op lokale MOI)[m
[32m+[m[32m    phi = (total_new_infections / (X_tot + 1e-10)) * 0.1 # Jouw MOI factor[m
[32m+[m[32m    prob_lys = getProbLys(phi)[m
[32m+[m
[32m+[m[32m    # Toxiciteit voor L-cellen[m
     toxic_death_L = p.k_tox * C_benz * L_cell[m
[31m-    toxic_death_S = (p.k_tox * 0.1) * C_benz * S_cell # S is minder gevoelig[m
[32m+[m
[32m+[m[32m    # Balansen voor I, L en P[m
[32m+[m[32m    du[p.ind_I] = (1 - prob_lys) * total_new_infections - lysis_term[m
[32m+[m[32m    du[p.ind_L] = p.mu * L_cell + (prob_lys * total_new_infections) - toxic_death_L[m
[32m+[m[32m    du[p.ind_P] = (p.b * lysis_term) - total_ads_loss[m
     # 4. DIFFERENTIAALVERGELIJKINGEN  [m
     for i in 1:length(p.ind_subs)[m
         sub_idx = p.ind_subs[i][m
[36m@@ -36,10 +106,17 @@[m [mfunction dFBA_phage_system(du, u, h, p::Parameters, t)[m
         # Als er door lysis weer glucose bijkomt (S_subs[i] > 1e-7), [m
         # zal de term p.q[i] * ... weer negatief worden en de glucose doen dalen.[m
         [m
[31m-        if S_subs[i] < 1e-8 && p.q[i] >= 0[m
[31m-             du[sub_idx] = 0.0[m
[32m+[m[32m        #if S_subs[i] < 1e-8 && p.q[i] >= 0[m
[32m+[m[32m             #du[sub_idx] = 0.0[m
[32m+[m[32m        #else[m
[32m+[m[32m             #du[sub_idx] = p.q[i] * p.E_coli_cellDW * X_tot[m[41m [m
[32m+[m[32m        #end[m
[32m+[m[32m        # Voorkom dat du[sub_idx] de concentratie negatief maakt[m
[32m+[m[32m        flux_val = p.q[i] * p.E_coli_cellDW * X_tot[m
[32m+[m[32m        if S_subs[i] <= 0.0 && flux_val > 0.0[m
[32m+[m[32m            du[sub_idx] = 0.0[m
         else[m
[31m-             du[sub_idx] = p.q[i] * p.E_coli_cellDW * X_tot [m
[32m+[m[32m            du[sub_idx] = flux_val[m
         end[m
         [m
         # Glucose vrijgave bij lysis (enkel voor index 1 = glucose)[m
[36m@@ -50,14 +127,9 @@[m [mfunction dFBA_phage_system(du, u, h, p::Parameters, t)[m
         du[enz_idx] = p.alpha_syn * f[i] * u_cyt[i] - p.beta_deg* e_enz[i] + 0.001 # kleine basale expressie zodat er altijd een beetje enzym is (voor snelle adaptatie bij nieuwe substraat beschikbaar)[m
     end[m
 [m
[31m-    # Faag-Host interactie[m
[31m-    du[p.ind_S] = p.mu * S_cell - nieuwe_infectie_flux - toxic_death_S[m
[31m-    du[p.ind_I] = (1 - prob_lys) * nieuwe_infectie_flux - lysis_term[m
[31m-    du[p.ind_L] = p.mu * L_cell + (prob_lys * nieuwe_infectie_flux) - toxic_death_L # raar dat er geen overshoot is van total X wat vreemd is, waarschijnlijk iets te maken met getphi ---> bekijk voor alternatief (misschien op andere manier doen, zonder growth speed initieel)[m
[31m-    du[p.ind_P] = (p.b * lysis_term) - totaal_adsorptie_flux [m
     # --- 4. BENZONASE BALANS ---[m
     # Productie (via p.q_benz uit FBA) minus afbraak[m
[31m-    du[p.ind_Benz] = (p.q_benz * p.E_coli_cellDW * X_tot) - (p.beta_benz * C_benz)[m
[32m+[m[32m    du[p.ind_Benz] = (p.q_benz * p.E_coli_cellDW * X_tot) #- (p.beta_benz * C_benz)[m
 end[m
 [m
 function getMonod(substrates::Vector{Float64}, parameters::Parameters)::Vector{Float64}[m
[1mdiff --git a/main.jl b/main.jl[m
[1mindex e082fa6..7e8814c 100644[m
[1m--- a/main.jl[m
[1m+++ b/main.jl[m
[36m@@ -17,14 +17,14 @@[m [mmodel = convert(AbstractFBCModels.CanonicalModel.Model, load_model(model_path))[m
 [m
 [m
 model.reactions["R_BIOMASS_Ec_iJO1366_core_53p95M"].lower_bound = 0.0 # bacterial growth is constrained!!![m
[31m-model.reactions["R_BIOMASS_Ec_iJO1366_core_53p95M"].upper_bound = 2.0 # [1/h] moet bovenlimiet op staan anders te hoog[m
[32m+[m[32mmodel.reactions["R_BIOMASS_Ec_iJO1366_core_53p95M"].upper_bound = 1.0 # [1/h] moet bovenlimiet op staan anders te hoog[m
 # 2. Cybernetische Parameters [m
 alpha_syn = 0.2*10   # Snelheid van enzymsynthese waardes van deze 2 nog opzoeken (stonden te laag)[m
 beta_deg = 0.05*10   # Snelheid van enzymdegradatie[m
 K_s = [0.0278, 0.0146, 0.0543, 0.0833]      # Affiniteit uit Tabel 7 (mmol/L)[m
 tau = 1.0          # Latente periode (uren)[m
 b = 170.0          # Burst size[m
[31m-alfa_ads = 1e-10  # Adsorptieconstante (L/gDW/h) lager dan bij Luan want eigenlijk meer deze waarde voor lambda bij te hoge alfa numerieke problemen)[m
[32m+[m[32malfa_ads = 1e-10  # Adsorptieconstante (L/gDW/h) lager dan bij Luan want eigenlijk meer deze waarde voor lambda bij te hoge alfa numerieke problemen), ik wil 2-step want moet voor hoge MOI zoals bij ons uiteindelijk gaat zijn[m
 p_pref = [0.8925, 0.08925,  0.008925,  0.008925] # Voorkeurshiërarchie uit thesis[m
 V_max = [15.0, 13.0, 11.0, 4.0]           # Opnamesnelheden (mmol/hr^-1)[m
 E_coli_cellDW = 2.8e-13 # gDW per cel[m
[36m@@ -35,39 +35,99 @@[m [mall_ex_ids     = [id for id in keys(model.reactions) if startswith(id, "R_EX_")][m
 MW_values      = [180.16, 342.3, 92.09, 60.05][m
 h_release = 5.66e-12 # glucose vrijgegeven bij lysis (mmol/burst) waarde gebaseerd op Luan table 7[m
 duration = 15.0[m
[32m+[m[32mn_hill = 2.0[m
[32m+[m[32m# K_mu = 0.2[m
[32m+[m[32mf_res = 0.05[m
[32m+[m[32mK_mal = 0.01[m
[32m+[m[32mmu_max_glc = 1.33[m
[32m+[m[32mmu_max_mal = 1.26[m
[32m+[m[32mmu_max_gly = 1.10[m
[32m+[m[32mmu_max_ac = 0.29[m
 # --- 2. De Struct aanmaken ---[m
 # Zorg dat de volgorde in je Parameters-file exact matcht met deze aanroep:[m
 include("./parameters.jl")[m
[32m+[m
[32m+[m[32m# Ladder configuratie[m
[32m+[m[32mmax_phages = 10[m[41m [m
[32m+[m
[32m+[m[32m# Definieer de indices voor de state vector u[m
[32m+[m[32m# 1:4   -> Substraten[m
[32m+[m[32m# 5:8   -> Enzymen[m
[32m+[m[32m# 9:19  -> S_ladder (S0, S1, S2, ..., S10) -> 11 variabelen[m
[32m+[m[32m# 20    -> I (Geïnfecteerd)[m
[32m+[m[32m# 21    -> L (Lysogeen)[m
[32m+[m[32m# 22    -> P (Vrije fagen)[m
[32m+[m[32m# 23    -> Benz (Benzonase)[m
[32m+[m
 p = Parameters([m
[31m-    alpha_syn, beta_deg, K_s, V_max, p_pref, tau, b, alfa_ads, E_coli_cellDW, MW_values, h_release, [m
[32m+[m[32m    # Cybernetica[m
[32m+[m[32m    alpha_syn,[m[41m [m
[32m+[m[32m    beta_deg,[m[41m [m
[32m+[m[32m    K_s,[m[41m [m
[32m+[m[32m    V_max,[m[41m [m
[32m+[m[32m    p_pref,[m[41m [m
[32m+[m[41m    [m
[32m+[m[32m    # Phage-Host[m
[32m+[m[32m    tau,[m[41m [m
[32m+[m[32m    b,[m[41m [m
[32m+[m[32m    E_coli_cellDW,[m[41m [m
[32m+[m[32m    MW_values,[m[41m [m
[32m+[m[32m    h_release,[m[41m [m
[32m+[m
[32m+[m[32m    # Model IDs[m
     "R_BIOMASS_Ec_iJO1366_core_53p95M", [m
[31m-    exchange_ids, all_ex_ids, essentials_ids, [m
[31m-    model, 0.0, zeros(length(exchange_ids)), [m
[31m-    1:4,  # ind_subs: nu correct op 1, 2, 3, 4[m
[31m-    5:8,  # ind_e:    nu correct op 5, 6, 7, 8[m
[31m-    9,    # ind_S[m
[31m-    10,   # ind_I[m
[31m-    11,   # ind_L[m
[31m-    12,   # ind_P[m
[32m+[m[32m    exchange_ids,[m[41m [m
[32m+[m[32m    all_ex_ids,[m[41m [m
[32m+[m[32m    essentials_ids,[m[41m [m
[32m+[m[32m    model,[m[41m [m
[32m+[m[32m    0.0,            # mu start op 0[m
[32m+[m[32m    zeros(4),       # q start op 0[m
[32m+[m
[32m+[m[32m    # Adsorption Ladder (Sequential Model)[m
[32m+[m[32m    max_phages,     # 10[m
[32m+[m[32m    1e-10,          # k_on (jouw alfa_ads)[m
[32m+[m[32m    10.0,           # k_off (1/h) -> Faag valt relatief snel los[m
[32m+[m[32m    5.0,            # k_ins (1/h) -> Gemiddeld 12 min voor injectie[m
[32m+[m[32m    K_mal,          # 0.01 (Verzadiging van LamB)[m
[32m+[m
[32m+[m[32m    # Indices[m
[32m+[m[32m    1:4,            # ind_subs[m
[32m+[m[32m    5:8,            # ind_e[m
[32m+[m[32m    9:19,           # ind_S_ladder[m
[32m+[m[32m    20,             # ind_I[m
[32m+[m[32m    21,             # ind_L[m
[32m+[m[32m    22,             # ind_P[m
     infection_time, [m
[31m-    13,                # ind_Benz (de 13e plek in u)[m
[31m-    "R_BENZ_prod",     # benz_id (moet matchen met de ID in je model)[m
[31m-    0.05,              # k_tox (begin hiermee en tweak later)[m
[31m-    0.1,               # beta_benz (enzymen breken langzaam af)[m
[31m-    0.0)                # q_benz (begint op 0))[m
[32m+[m
[32m+[m[32m    # Benzonase[m
[32m+[m[32m    23,             # ind_Benz[m
[32m+[m[32m    "R_BENZ_prod",[m[41m [m
[32m+[m[32m    0.05,           # k_tox[m
[32m+[m[32m    0.1,            # beta_benz[m
[32m+[m[32m    0.0,            # q_benz start op 0[m[41m    [m
[32m+[m[41m    [m
[32m+[m[32m    # Luan Referenties[m
[32m+[m[32m    mu_max_glc,[m[41m [m
[32m+[m[32m    mu_max_mal,[m[41m [m
[32m+[m[32m    mu_max_gly,[m[41m [m
[32m+[m[32m    mu_max_ac)[m
 [m
 include("./dFBA_function.jl")[m
 # 5. INITIALISATIE[m
 # [Glc, Mal, Glyc, Ac, e_glc, e_mal, e_Glyc, e_ac, S, I, L, P, Benz] (subs in mmol/l)[m
[31m-u0 = [4.44, 2.337, 5.42, 0.0, 0.95, 0.01, 0.01, 0.01, 1e6, 0.0, 0.0, 0.0, 0.0] #startwaardes komen niet overeen met plot wat vreemd is + er gebeurt niets met maltose --> fix![m
[31m-# 1. Maak de stoichiometrie aan (wie gaat erin, wie komt eruit)[m
[32m+[m[32mu0 = zeros(23)[m
[32m+[m[32mu0[1:4] = [4.44, 2.337, 5.42, 0.0]    # Subs[m
[32m+[m[32mu0[5:8] = [0.95, 0.01, 0.01, 0.01]    # Enzymen[m
[32m+[m[32mu0[9]   = 1e6                         # S0 (alle cellen beginnen zonder fagen)[m
[32m+[m[32mu0[10:19] .= 0                         # S1 t/m S10[m
[32m+[m[32mu0[20:23]  .= 0                         # I[m
 # Let op: check in je iJO1366.xml of het "atp_c" of "atp[c]" is![m
 benz_stoich = Dict([m
[31m-    "M_atp_c" => -20.0, [m
[31m-    "M_h2o_c" => -20.0, [m
[31m-    "M_adp_c" =>  20.0, [m
[31m-    "M_pi_c"  =>  20.0, [m
[31m-    "M_h_c"   =>  20.0[m
[32m+[m[32m    "M_atp_c" => 0,[m[41m [m
[32m+[m[32m    "M_h2o_c" => 0,[m[41m [m
[32m+[m[32m    "M_adp_c" =>  0,[m[41m [m
[32m+[m[32m    "M_pi_c"  =>  0,[m[41m [m
[32m+[m[32m    "M_h_c"   =>  0[m
 )[m
 [m
 # 2. Bouw het officiële Reaction object[m
[36m@@ -92,13 +152,23 @@[m [mfbaAffect!(integrator) = fbaUpdate!(integrator.u, p)[m
 fbaCallBack = DiscreteCallback(fbaUpdateCondition, fbaAffect!)[m
 [m
 prob = DDEProblem(dFBA_phage_system, u0, (p,t)->u0, tspan, p)[m
[31m-[m
 sol = solve(prob, MethodOfSteps(Tsit5()), [m
[31m-            reltol=1e-6, # Zet deze weer iets strenger voor detail[m
[31m-            abstol=1e-8,[m
[32m+[m[32m            reltol=1e-3, # Zet deze weer iets strenger voor detail[m
[32m+[m[32m            abstol=1e-5,[m
[32m+[m[32m            maxiters=1e5,[m
             tstops=[p.infection_time; fbaUpdateTimepoints],[m
[31m-            callback=CallbackSet(infectionCallBack, fbaCallBack))[m
[31m-            #met isoutofdomain stopt solver telkens na 6 uur omdat glucose onder 0 gaat, dus nu met discrete callback die elke 0.1 uur FBA update doet en daarin zetten we constraint op glucose op 0 als die onder 0 gaat, werkt goed en sneller dan zonder discrete callback (omdat we niet telkens opnieuw moeten oplossen maar gewoon de constraints aanpassen in het model)           [m
[32m+[m[32m            callback=CallbackSet(infectionCallBack, fbaCallBack),[m
[32m+[m[32m            isoutofdomain=(u,p,t) -> any(x -> x < -1e-8, u))[m
[32m+[m[32m            #met isoutofdomain stopt solver telkens na 6 uur omdat glucose onder 0 gaat, dus nu met discrete callback die elke 0.1 uur FBA update doet en daarin zetten we constraint op glucose op 0 als die onder 0 gaat, werkt goed en sneller dan zonder discrete callback (omdat we niet telkens opnieuw moeten oplossen maar gewoon de constraints aanpassen in het model)[m[41m   [m
[32m+[m[32m        #sol = solve(prob, MethodOfSteps(Rosenbrock23(autodiff=false)),[m[41m [m
[32m+[m[32m            #reltol=1e-3,[m[41m [m
[32m+[m[32m            #abstol=1e-5,[m
[32m+[m[32m            #maxiters=1e6, # Verhoogd om niet te vroeg te stoppen[m
[32m+[m[32m            #tstops=[p.infection_time; fbaUpdateTimepoints],[m
[32m+[m[32m            #callback=CallbackSet(infectionCallBack, fbaCallBack),[m
[32m+[m[32m            #isoutofdomain=(u,p,t)->any(x->x<0, u)) # Voorkomt negatieve populaties[m[41m [m
[32m+[m[32m# Gebruik een stijvere solver met betere stabiliteit[m
[32m+[m[41m          [m
 # 6. UITGEBREID PLOTTEN[m
 # Plot A: Substraatverloop[m
 p1 = plot(sol, idxs=p.ind_subs, title="Substraten (mmol/L)", [m
[36m@@ -110,10 +180,16 @@[m [mp2 = plot(sol, idxs=p.ind_e, title="Enzym-niveaus (Cybernetische e)",[m
 [m
 [m
 # Plot C: Host populatie[m
[31m-p3 = plot(sol.t, [getTotalBiomass(u, p) for u in sol.u], title="Bacteriële Populatie", yscale=:log10, ylims=(1,:auto),[m
[32m+[m[32m# Bereken data voor de plots[m
[32m+[m[32mS_totaal_data = [sum(u[p.ind_S_ladder]) for u in sol.u][m
[32m+[m[32mX_totaal_data = [getTotalBiomass(u, p) for u in sol.u][m
[32m+[m
[32m+[m[32mp3 = plot(sol.t, X_totaal_data, title="Bacteriële Populatie", yscale=:log10, ylims=(1,:auto),[m
           label="Totaal X", color=:black, lw=3, ylabel="cells [1/L]")[m
[31m-plot!(p3, sol, idxs=[p.ind_S, p.ind_I], xlabel= "t [h]", label=["Vatbaar (S)" "Infect (I)"], alpha=0.7)[m
 [m
[32m+[m[32m# Hier plotten we de som van S0 t/m S10[m
[32m+[m[32mplot!(p3, sol.t, S_totaal_data, label="Totaal Vatbaar (S)", color=:blue, lw=2)[m
[32m+[m[32mplot!(p3, sol, idxs=[p.ind_I, p.ind_L], label=["Infect (I)" "Lysogeen (L)"], alpha=0.7)[m
 [m
 # Plot D: Faag Lambda Dynamiek[m
 p4 = plot(sol.t, [getPhages(u, p) for u in sol.u], title="Fagen (P)", yscale=:log10, ylims=(1,:auto),[m
[36m@@ -127,9 +203,23 @@[m [mp5 = plot(sol.t, [u[p.ind_Benz] for u in sol.u],[m
           xlabel="t [h]", ylabel="Conc. [mmol/L]",[m
           label="Benzonase")[m
 [m
[31m-# Combineer alle plots in een nieuwe layout[m
[31m-plot(p1, p2, p3, p4, p5, layout=(3,2), size=(900,1000), margin=5Plots.mm)[m
[32m+[m[32m# Plot voor de individuele ladder-treden[m
[32m+[m[32mp6 = plot(title="Adsorptie Ladder Verdeling", yscale=:log10,[m[41m [m
[32m+[m[32m          xlabel="t [h]", ylabel="cells [1/L]", ylims=(1, :auto))[m
 [m
[32m+[m[32m# We loopen door de treden om ze allemaal toe te voegen[m
[32m+[m[32mfor i in 0:p.max_phages[m
[32m+[m[32m    # i=0 is S0, i=1 is S1, etc.[m
[32m+[m[32m    label_name = "S$i"[m
[32m+[m[32m    # Gebruik een kleurverloop van blauw naar rood voor de bezettingsgraad[m
[32m+[m[32m    plot!(p6, sol, idxs=p.ind_S_ladder[i+1], label=label_name, alpha=0.6)[m
[32m+[m[32mend[m
[32m+[m
[32m+[m[32m# Combineer alle plots in een nieuwe layout[m
[32m+[m[32mplot(p1, p2, p3, p4, p5, p6,[m[41m [m
[32m+[m[32m                  layout=(3, 2),[m[41m [m
[32m+[m[32m                  size=(1000, 1200), # Iets groter gemaakt voor de leesbaarheid[m
[32m+[m[32m                  margin=5Plots.mm)[m
 #volgende stap is nu productie erin te krijgen en in uw FBA inbouwen door bijvoorbeeld zelf reacties toe te voegen zoals in stoich matrix (bijvoorbeeld met insuline/benzoase (enzym misschien goede eerste stap, moet niet per se aan FBA omdat enzym) --> welke enzymes etc zijn nodig in E. Coli hiervoor en welke substraten moeten voor deze productie worden opgenomen, zie paper in edge)[m
 #branch maken in github ook dat je terugkan als het niet lukt[m
 # als ik dat heb zoek op optimization.jl (zoek package en neem door)[m
\ No newline at end of file[m
[1mdiff --git a/parameters.jl b/parameters.jl[m
[1mindex 3d10ea2..90d08af 100644[m
[1m--- a/parameters.jl[m
[1m+++ b/parameters.jl[m
[36m@@ -9,7 +9,6 @@[m [mmutable struct Parameters[m
     # Phage-Host Parameters[m
     tau::Float64[m
     b::Float64[m
[31m-    alfa_ads::Float64[m
     E_coli_cellDW::Float64[m
     MW::Vector{Float64}[m
     h_release::Float64[m
[36m@@ -23,10 +22,17 @@[m [mmutable struct Parameters[m
     mu::Float64[m
     q::Vector{Float64}[m
 [m
[32m+[m[32m    # --- NIEUWE ADSORPTIE PARAMETERS (LADDER) ---[m[41m[m
[32m+[m[32m    max_phages::Int64    # Hoeveel fagen kunnen er op 1 cel? (bijv. 10)[m[41m[m
[32m+[m[32m    k_on::Float64        # Binding rate (L / gDW / h) - voorheen alfa_ads[m[41m[m
[32m+[m[32m    k_off::Float64       # Loskoppelingsrate (1/h)[m[41m[m
[32m+[m[32m    k_ins::Float64       # Injectierate (1/h) - de stap van S_i naar I[m[41m[m
[32m+[m[32m    K_mal::Float64       # Affiniteit voor LamB (verzadiging van de ladder)[m[41m[m
[32m+[m[41m[m
     # Indices voor state vector u[m
     ind_subs::Vector{Int64}[m
     ind_e::Vector{Int64}[m
[31m-    ind_S::Int64[m
[32m+[m[32m    ind_S_ladder::UnitRange{Int} # S0, S1, ..., S10[m[41m[m
     ind_I::Int64[m
     ind_L::Int64[m
     ind_P::Int64[m
[36m@@ -38,53 +44,64 @@[m [mmutable struct Parameters[m
     k_tox::Float64     # Toxiciteitscoëfficiënt[m
     beta_benz::Float64 # Afbraaksnelheid enzym[m
     q_benz::Float64    # Opslag voor de berekende flux[m
[32m+[m[41m[m
[32m+[m[32m    # Referentie mu_max waarden uit Luan[m[41m[m
[32m+[m[32m    mu_max_glc::Float64   # 1.33[m[41m[m
[32m+[m[32m    mu_max_mal::Float64   # 1.26[m[41m[m
[32m+[m[32m    mu_max_gly::Float64   # 1.10[m[41m[m
[32m+[m[32m    mu_max_ac::Float64    # 0.29[m[41m[m
 end[m
 [m
[31m-# Berekent de lokale MOI (phi): de verhouding tussen infectiesnelheid en groeisnelheid[m
[32m+[m[41m[m
[32m+[m[32m# 2. Receptor-factor (Storms: Michaelis-Menten verzadiging van LamB)[m[41m[m
[32m+[m[32m#function f_receptor(u, p::Parameters)[m[41m[m
[32m+[m[32m    # We halen het relatieve niveau van maltose-enzymen op[m[41m[m
[32m+[m[32m    #e_mal = u[p.ind_e[2]][m[41m [m
[32m+[m[32m    #return e_mal / (p.K_mal + e_mal + 1e-15)[m[41m[m
[32m+[m[32m#end[m[41m[m
[32m+[m[41m[m
[32m+[m[41m[m
[32m+[m[32m## --- POPULATIE DYNAMICA ---[m[41m[m
[32m+[m[41m[m
 function getPhi(u, p::Parameters)[m
[31m-    alfa_eff = getAlfa_eff(u, p)[m
[31m-    P_phage = u[p.ind_P][m
[31m-    return (alfa_eff * P_phage) * 0.1[m
[32m+[m[32m    # Totaal aantal fagen gebonden aan cellen[m[41m[m
[32m+[m[32m    fagen_op_cellen = sum([i * u[p.ind_S_ladder[i+1]] for i in 1:p.max_phages])[m[41m[m
[32m+[m[32m    X_tot = getTotalBiomass(u, p)[m[41m[m
[32m+[m[32m    return fagen_op_cellen / (X_tot + 1e-15)[m[41m[m
 end[m
 [m
[31m-# Berekent de kans op lysogenie op basis van phi[m
 function getProbLys(phi::Float64)[m
[31m-    # Poisson n >= 2: 1 - P(0) - P(1)[m
[31m-    # P(0) = e^-phi, P(1) = phi * e^-phi[m
     return 1 - exp(-phi) - (phi * exp(-phi))[m
 end[m
 [m
 function getTotalBiomass(u, p::Parameters)[m
[31m-    return u[p.ind_S] + u[p.ind_I] + u[p.ind_L][m
[31m-end[m
[31m-[m
[31m-function getPhages(u, p::Parameters)::Float64 [m
[31m-    return u[p.ind_P][m
[32m+[m[32m    # Som van de ladder (S0 t/m S10) + geïnfecteerd + lysogeen[m[41m[m
[32m+[m[32m    return sum(u[p.ind_S_ladder]) + u[p.ind_I] + u[p.ind_L][m[41m[m
 end[m
 [m
[31m-function getTotalAdsorptionFlux(u, p::Parameters)[m
[31m-    X_tot = getTotalBiomass(u, p)[m
[31m-    alfa_eff = getAlfa_eff(u, p)[m
[31m-    return alfa_eff * X_tot * u[p.ind_P][m
[31m-end[m
[32m+[m[32m#function getTotalAdsorptionFlux(u, mu_werkelijk, u_cyt, p::Parameters)[m[41m[m
[32m+[m[32m    #X_tot = getTotalBiomass(u, p)[m[41m[m
[32m+[m[32m    #alpha_nu = get_alpha_eff(u, mu_werkelijk, u_cyt, p)[m[41m[m
[32m+[m[32m    #return alpha_nu * X_tot * u[p.ind_P][m[41m[m
[32m+[m[32m#end[m[41m[m
 [m
[31m-function getNewInfectionFlux(u, p::Parameters)[m
[31m-    alfa_eff = getAlfa_eff(u, p)[m
[31m-    return alfa_eff * u[p.ind_S] * u[p.ind_P][m
[31m-end[m
[32m+[m[32m#function getNewInfectionFlux(u, mu_werkelijk, u_cyt, p::Parameters)[m[41m[m
[32m+[m[32m    #alpha_nu = get_alpha_eff(u, mu_werkelijk, u_cyt, p)[m[41m[m
[32m+[m[32m    #return alpha_nu * u[p.ind_S] * u[p.ind_P][m[41m[m
[32m+[m[32m#end[m[41m[m
 [m
[31m-function getAlfa_eff(u, p::Parameters)[m
[31m-    e_mal = u[p.ind_e[2]] # Stel dat maltose het 2e substraat is[m
[31m-    # e_mal loopt van 0 tot 1 (relatief enzymniveau)[m
[31m-    # We koppelen dit aan de maximale alfa_ads[m
[31m-    base_leak = 0.001 # 0.1% basale expressie[m
[31m-    return p.alfa_ads * (base_leak + (1 - base_leak) * e_mal)[m
[31m-end[m
[32m+[m[32m#function getAlfa_eff(u, p::Parameters)[m[41m[m
[32m+[m[32m    #e_mal = u[p.ind_e[2]] # Stel dat maltose het 2e substraat is[m[41m[m
[32m+[m[32m    #e_mal loopt van 0 tot 1 (relatief enzymniveau)[m[41m[m
[32m+[m[32m    #We koppelen dit aan de maximale alfa_ads[m[41m[m
[32m+[m[32m    #base_leak = 0.001 # 0.1% basale expressie[m[41m[m
[32m+[m[32m    #return p.alfa_ads * (base_leak + (1 - base_leak) * e_mal)[m[41m[m
[32m+[m[32m#end[m[41m[m
 [m
 function fbaUpdate!(u, p::Parameters)[m
     S_subs = u[p.ind_subs][m
     e_enz  = u[p.ind_e][m
[31m-[m
[32m+[m[32m    println("t = ???, mu = $(p.mu), X_tot = $(getTotalBiomass(u, p))")[m[41m[m
     # --- 1. CYBERNETISCHE BEREKENINGEN ---[m
     f = getMonod(S_subs, p)[m
     R = getRate(f, p)[m
[36m@@ -104,7 +121,7 @@[m [mfunction fbaUpdate!(u, p::Parameters)[m
                 [m
                 # Numerieke veiligheid: nooit meer opnemen dan er fysiek is[m
                 # (S_subs / (biomassa * dt)) - we houden het simpel:[m
[31m-                v_max_available = conc / (p.E_coli_cellDW * getTotalBiomass(u, p) + 1e-10)[m
[32m+[m[32m                v_max_available =  conc / (p.E_coli_cellDW * getTotalBiomass(u, p) + 1e-10)[m[41m[m
                 [m
                 # De lower_bound is negatief voor opname[m
                 p.fbaModel.reactions[id].lower_bound = -min(v_max_kin, v_max_available)[m
[36m@@ -141,4 +158,14 @@[m [mfunction fbaUpdate!(u, p::Parameters)[m
         p.q .= 0.0[m
         p.q_benz = 0.0[m
     end[m
[32m+[m[32mend[m[41m[m
[32m+[m[41m[m
[32m+[m[32mfunction getPhages(u, p::Parameters)[m[41m[m
[32m+[m[32m    return u[p.ind_P][m[41m[m
[32m+[m[32mend[m[41m[m
[32m+[m[41m[m
[32m+[m[32mfunction getTotalPhages(u, p::Parameters)[m[41m[m
[32m+[m[32m    free_phages = u[p.ind_P][m[41m[m
[32m+[m[32m    bound_phages = sum([i * u[p.ind_S_ladder[i+1]] for i in 1:p.max_phages])[m[41m[m
[32m+[m[32m    return free_phages + bound_phages[m[41m[m
 end[m
\ No newline at end of file[m
